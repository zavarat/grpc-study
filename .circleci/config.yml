version: 2.1
jobs:
  build:
    docker:
      - image: alpine:3.7
    steps:
      - checkout
      - run:
          name: The first step
          command: |
            echo 'Hello World'!
            echo 'This is delivery pipeline'
      - run:
          name: code has arrived
          command: |
            ls -al
            echo 'end!!'
      